git log所作用的scope是branch，每一个分支有自己所属的独特的log记录.
git tag作用的对象是commit记录.
git log --graph --oneline --decorate   高逼格的查看log记录.除此之外，使得查看的内容更加可读，very good. truely!

#有讲究的拷贝
	#指定tag版本
git clone --branch v1.1.0 <repo>
	#指定拷贝最新的commit
git clone -depth=1 <repo>






